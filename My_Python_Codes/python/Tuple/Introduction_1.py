#Tuples are immutable
#no pop, no append, no del.,no remove
#we can not change data in tuple
#Tuples are faster

#methods:    count,index,slicing
#Functions    min(),max(),index(),len(),sum()

# x=(1,2,'s',5)
# y=(4,)   # comma is required at last to insert a single character in tuple
# a,b,c,d=(x)   #unpacking
# print(b,d,c)

# list inside tuple we can use append,pop,remove,del  on list not on tuple
# q=(1,2,3,'a',[4,5,6,'b',7])
# q[4].pop()
# # print(q)
# l=(34,45,54,23)
# print(min(l))

# def fun(int1,int2):
#     add =int1+int2
#     multiply=int1*int2
#     return(add,multiply)
# print(fun(3,54))
# a,b=fun(3,6)  # unpacking of tuple
# print(type(a))
# print(b)

# keys=input("enter the keys you want to have in your dictionary(seprated by comma)")
# keys=keys.split()
# values=input("enter the values you want to have in your dictionary(seprated by comma)")
# values=values.split
# d={}
# for i in range (len(keys)):
#     d[keys[i]]=values[i]
# print(d)



