# git push -u origin main    (to set upstream, in future if we will write only git push)
# git checkout -b feature1 (To create new branch with nsme feature1)
# git checkout branch name (To Navigate)
# git branch -d branch name (To delete branch)
# to delete any branch we should be in other branch otherwise error throw hoga